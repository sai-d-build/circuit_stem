# Test Coverage Notes

Cannot run `flutter test --coverage`; estimated from structure: ~50 unit/widget tests (game_state, drag_drop, use_cases), 5 integration (canvas, game_flow), no e2e. Coverage ~60% (lib/ focused on core/game, but presentation/UI low). lcov.info would be generated by CI.

Unit: 40, Widget: 10, Integration: 5.

Recommend user runs for actual lcov.info.